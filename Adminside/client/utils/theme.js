import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#002131',
        },
        secondary: {
            main: '#d62828',
        },
        type: 'dark',
    },
});

export default theme;
